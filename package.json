{
  "name": "my-web-data-workflow",
  "private": true,
  "scripts": {
    "data:build": "node scripts/data/build.cjs",
    "data:check": "node scripts/data/build.cjs --check",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "cheerio": "1.2.0"
  }
}
